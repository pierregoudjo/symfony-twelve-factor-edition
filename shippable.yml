language: php
php:
  - 5.6
before_script:
  - cp .env.example .env
  - curl -sS https://getcomposer.org/installer | php -- --install-dir=bin --filename=composer
  - bin/composer install --prefer-dist
notifications:
  email: false
env:
 - DSN_URL=sqlite:///database MAILER_TRANSPORT=smtp MAILER_HOST=127.0.0.1 MAILER_USER=user MAILER_PASSWORD=password SECRET=ThisTokenIsNotSoSecretChangeIt SYMFONY_ENV=dev SYMFONY_DEBUG=true LOG_LEVEL=info
